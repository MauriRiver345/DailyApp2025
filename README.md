# DailyApp2025
The DailyApp for Web Services

# Instructions
## .NET
For running the C# API projectv you need to navigate `./API` and execute commands:

1. `dotnet restore`
2. `dotnet run`

## Angular
For running the Angular app you need to navigate `./client` and execute commands:

1. `npm install`
2. `ng serve`
